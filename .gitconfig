[ui]
	color = auto
[color]
	ui = true
[alias]
	br = branch
	co = checkout
	who = shortlog -s --
	last = cat-file commit HEAD
	ds = diff --stat --name-status
	dsc = diff --stat --name-status --cached
	up = "!git remote update ; git gc"
	lc = log ORIG_HEAD.. --stat --no-merges
	undo = reset --hard
	ss = stash save
	sp = stash pop
	sls = stash list
[diff]
	patience = true
